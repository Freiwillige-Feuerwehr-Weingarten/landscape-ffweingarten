version: '3.1'

services:

  adminer:
    image: adminer:5.4.0
    restart: always
    ports:
      - 8100:8080
    environment:
        ADMINER_DESIGN: pepa-linha
        ADMINER_DEFAULT_SERVER: psql.prod.ff-weingarten.de