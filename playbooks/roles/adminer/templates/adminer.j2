version: '3.1'

services:

  adminer:
    image: adminer
    restart: always
    ports:
      - 8100:8080
    environment:
        ADMINER_DESIGN: pepa-linha
        ADMINER_DEFAULT_SERVER: psql.prod.ff-weingarten.de